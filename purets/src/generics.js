var score = [];
var names = [];
function identityOne(val) {
    return val;
}
function identityTwo(val) {
    return val;
}
// Im ready to accept anything and give it that type
function identityTree(val) {
    return val;
}
// The value u gave is 3 and it will return u 3 as a number
identityTree("3");
// OR
function identityFour(val) {
    return val;
}
// indentityFour<Boole>({})
function getSearchProducts(_a) {
    // do some database operations
    var products = _a[0], T = _a[1], _b = _a[2];
    var myIndex = 3;
    return products[3];
}
// anytype of array
var getMoreSearchProducts = function (products) {
    // do somedataBase operations
    var myIndex = 4;
    return products[myIndex];
};
