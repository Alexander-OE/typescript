// used to restrict a users choice

const enum SeatChoice {
    AISLE = "aisle",
    // PUT A STRICT NIMBER AND THE INCREASING ORDER WILL FOLLOW
    MIDDLE = "middle",
    WINDOW ,
    FOUTH
}

const shcSeat = SeatChoice.AISLE